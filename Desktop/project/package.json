{
  "name": "legacy-app",
  "private": true,
  "version": "1.0.0",
  "description": "Legacy death confession app",
  "scripts": {
    "dev": "cd legacy-app && npm run dev",
    "build": "cd legacy-app && npm run build",
    "start": "cd legacy-app && npm start",
    "lint": "cd legacy-app && npm run lint"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.8"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
